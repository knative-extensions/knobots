shortName: gofmt
title: "Update actions"

# No special inputs needed.

prTitle: "[${{ steps.inferbranch.outputs.branch }}] Update actions"
repos:
  main:
    name: "${{ matrix.name }}"
  meta:
    name: "${{ matrix.meta-organization }}/.github"

commitMessage: |
  Update actions

  ${{ steps.work.outputs.log }}