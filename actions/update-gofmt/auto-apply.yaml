shortName: gofmt
title: "Run Gofmt and Goimport"

# No special inputs needed.

prTitle: "[${{ steps.inferbranch.outputs.branch }}] Format Go code"

commitMessage: |
  Format go code

  ${{ steps.work.outputs.log }}